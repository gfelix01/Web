# Limit concurrent operations to reduce memory pressure
maxsockets=5
fetch-retries=3
fetch-retry-mintimeout=5000
fetch-retry-maxtimeout=120000

# Ensure we don't run out of memory during install operations
jobs=2
